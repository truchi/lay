#!/bin/sh

if [ "$1" = "watch" ]; then
  cargo watch      \
    -w generation/ \
    -s "clear; ./gen"
else
  (cd generation && cargo run --quiet)
  rustfmt src/**.rs
  rustfmt examples/**.rs
  cargo check --quiet
  ./doc
fi
