////////////////////////////////////////////////////////////////////////////////
// 🚨🚨🚨🚨🚨🚨🚨🚨 This file is @generated by build script. 🚨🚨🚨🚨🚨🚨🚨🚨 //
// 🚧🚧🚧🚧🚧🚧🚧🚧           ⛔ DO NOT MODIFY! ⛔           🚧🚧🚧🚧🚧🚧🚧🚧 //
////////////////////////////////////////////////////////////////////////////////

use crate::*;

/// A [`Background`](crate::Background) [`Color`](crate::Color).
///
/// Prints the corresponding CSI to the terminal when `Display`ed.
///
/// `Default`s to `Background(Color::ResetColor)`, user's default terminal's
/// background color.
#[derive(Copy, Clone, Eq, PartialEq, Hash, Debug)]
pub struct Background(pub Color);

/// `Default`s to `Background(Color::ResetColor)`.
impl Default for Background {
    /// `Default`s to `Background(Color::ResetColor)`.
    fn default() -> Self {
        Background(Color::ResetColor)
    }
}

/// Returns `Background(Color)`.
impl From<Color> for Background {
    /// Returns `Background(Color)`.
    fn from(color: Color) -> Self {
        Background(color)
    }
}

/// Returns `Some(Background(Color))`.
impl From<Color> for Option<Background> {
    /// Returns `Some(Background(Color))`.
    fn from(color: Color) -> Self {
        Some(Background(color))
    }
}

impl StylerIndex for Background {
    /// Returns `None`.
    fn get_foreground(&self) -> Option<Foreground> {
        None
    }

    /// Returns `Some(self)`.
    fn get_background(&self) -> Option<Background> {
        Some(*self)
    }

    /// Returns `None`.
    fn get_weight(&self) -> Option<Weight> {
        None
    }

    /// Returns `None`.
    fn get_slant(&self) -> Option<Slant> {
        None
    }

    /// Returns `None`.
    fn get_underline(&self) -> Option<Underline> {
        None
    }

    /// Returns `None`.
    fn get_strike(&self) -> Option<Strike> {
        None
    }

    /// Returns `None`.
    fn get_overline(&self) -> Option<Overline> {
        None
    }

    /// Returns `None`.
    fn get_invert(&self) -> Option<Invert> {
        None
    }

    /// Returns `None`.
    fn get_blink(&self) -> Option<Blink> {
        None
    }

    /// Returns `None`.
    fn get_border(&self) -> Option<Border> {
        None
    }
}

impl Styler for Background {
    type Output = Style;

    /// Returns a `Style` with `background` (self) and `foreground`.
    fn foreground(self, foreground: impl Into<Option<Foreground>>) -> Self::Output {
        Style {
            background: Some(self),
            foreground: foreground.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background`.
    fn background(self, background: impl Into<Option<Background>>) -> Self::Output {
        Style {
            background: background.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `weight`.
    fn weight(self, weight: impl Into<Option<Weight>>) -> Self::Output {
        Style {
            background: Some(self),
            weight: weight.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `slant`.
    fn slant(self, slant: impl Into<Option<Slant>>) -> Self::Output {
        Style {
            background: Some(self),
            slant: slant.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `underline`.
    fn underline(self, underline: impl Into<Option<Underline>>) -> Self::Output {
        Style {
            background: Some(self),
            underline: underline.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `strike`.
    fn strike(self, strike: impl Into<Option<Strike>>) -> Self::Output {
        Style {
            background: Some(self),
            strike: strike.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `overline`.
    fn overline(self, overline: impl Into<Option<Overline>>) -> Self::Output {
        Style {
            background: Some(self),
            overline: overline.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `invert`.
    fn invert(self, invert: impl Into<Option<Invert>>) -> Self::Output {
        Style {
            background: Some(self),
            invert: invert.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `blink`.
    fn blink(self, blink: impl Into<Option<Blink>>) -> Self::Output {
        Style {
            background: Some(self),
            blink: blink.into(),
            ..Style::NONE
        }
    }

    /// Returns a `Style` with `background` (self) and `border`.
    fn border(self, border: impl Into<Option<Border>>) -> Self::Output {
        Style {
            background: Some(self),
            border: border.into(),
            ..Style::NONE
        }
    }
}
